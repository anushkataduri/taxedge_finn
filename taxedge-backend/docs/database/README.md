# Database docs

Flyway migrations live in `src/main/resources/db/migration`, one numbered
file per module change (see naming convention there). ER diagrams and
schema notes for each module can go here.
