spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/taxedge
    username: postgres
    password: Srini123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
logging:
  level:
    com.taxedge: DEBUG