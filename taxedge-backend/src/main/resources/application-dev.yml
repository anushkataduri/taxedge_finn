spring:
  datasource:
    url: jdbc:mysql://localhost:3306/taxedge
    username: root
    password: "shashi/4274"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  flyway:
    baseline-on-migrate: true
