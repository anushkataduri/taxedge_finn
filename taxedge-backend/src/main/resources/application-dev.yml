# spring:
#   datasource:
#     url: jdbc:postgresql://localhost:5432/taxedge_db
#     username: postgres
#     password: ${DB_PASSWORD:Srini123}

#   jpa:
#     hibernate:
#       ddl-auto: update
#     show-sql: true

spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/taxedge_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: root
    password: shashi/4274

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 150MB
